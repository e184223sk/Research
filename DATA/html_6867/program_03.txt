
List&lt;Class&gt; list = new List&lt;Class&gt;();     //    これが元リスト
list.Add(new Class(～～));
    ：

foreach (Class item in list.ToArray())    //    エラーにならないんですよー、これが
    if (要らないモンみーつけた)
        list.Remove(item);

