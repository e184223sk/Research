
                /* ランニングステータスチェック */
                if (data[i] &lt; 0x80)
                {
                    // ランニングステータス適応(前回のステータスバイトを使いまわす)
                }
                else
                {
                    // ステータスバイト保存
                    statusByte = data[i++];
                }

