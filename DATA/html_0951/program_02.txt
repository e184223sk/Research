
        //赤くするのサブルーチン
        private void Set_Color(TextBox textBox)
        {
            if( textBox.Text != "0")
            {
                textBox.ForeColor = Color.Red;
            }
        }

