
[HttpPost]
[ValidateAntiForgeryToken]
public ActionResult Index(string cmd)
{
    if (cmd == "検索")
    {
        // 検索ボタンが押された場合の処理
    } 

    if (cmd == "クリア")
    {
        // クリアボタンが押された場合の処理
    } 

