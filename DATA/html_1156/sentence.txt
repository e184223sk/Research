More than 1 year has passed since last update.LINQにはラムダで条件式を渡し、要素全てがその条件を満たしているかを判定してboolを返す、
All()という便利なメソッドがあります。空のリストに対してこのメソッドを実行すると、直感とは異なる挙動になります。
（「全てが満たす」だからfalseになると思っていた）All()の実装を見てみると、リストの要素が空の場合は必ずtrueが返るようになっていました。
Any()等を組み合わせて判定するのが安全そうです。


