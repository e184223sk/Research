More than 1 year has passed since last update.Unity2017.4.3f1テストメソッドにつける。返り値はvoidでなくてはならないテストメソッドにつける。Unity専用。IEnumeratorでなくてはならないそのクラスの各テストが開始される前に実行されるそのクラスの各テストが終了した後に実行される。SetUp必須そのクラスのテストが開始される前に一度だけ実行されるそのクラスの全てのテストが終了した後に実行される。OneTimeSetUp必須テストの実行の優先度を指定する。クラス内でのみ有効テストを実行しない実行結果テストする値を指定するstaticフィールドをテストする値として使用する引数にそれぞれテストする値を指定するstaticフィールドをテストケースとして使用するテストする値を範囲で指定するテストする値をランダムで指定するテストケースの組み合わせ方の指定テストを指定した回数繰り返す。途中でテストに失敗したら繰り返しを止めるテストが失敗したら、指定した回数内で成功するまで再実行する指定した時間経過すると処理を中断し、失敗にする指定した時間経過すると失敗扱いにする。Timeoutと違い、処理は中断されないTheoryで妥当性を検証する変数に付けるTheoryで妥当性を検証するフィールドに付けるDataPoint,DataPointSourceの妥当性を検証する個別に実行しない限り実行されない（="Run All"から除外される）テストの説明文をつけるテストにカテゴリーを指定する。TestRunnerではカテゴリーごとにテストを実行する/しないの指定ができるテストを実行する環境を制限する。OS、アーキテクチャ、ランタイムを指定できる。カンマ区切りで複数指定可能


