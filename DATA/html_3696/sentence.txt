More than 1 year has passed since last update.問題のリンク名前 $name(i)$ と必要な処理時間 $time(i)$ を持つ $n$ 個のプロセスが順番に一列に並んでいる。各プロセス $q$ msだけ処理を実行する。プロセスが完了しなければ、列の最後尾に移動し、次のプロセスに処理が移行する。全てのプロセスが終了するまで、プロセスが完了した順に名前と処理が終了した時間を出力せよ。$q$ ms以内に処理が完了しなければデータは再びコレクションに戻るので、データを先頭から取り出し、末尾に詰めることができるキューを使う。先頭のデータが $q$ 以下か $q$ より大きいかでデータに対する処理が変わるのでifで場合分けすればよい。 以下ロジックを簡潔に示したもの。$time$は累積していくので毎回足していくこと。また今回の場合、出力に$name$と$time$がある。これを一つの$queue$で管理するのはめんどくさいので、$queue$を二つ作り、$name$と$time$を別のデータ構造の保管し、同時に処理を加えていくこと。


