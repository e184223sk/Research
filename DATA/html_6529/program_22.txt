
    // Dictionary&lt;TKey, TValue&gt;
    var dic = new Dictionary&lt;int, string&gt;();
    int key = 0;
    dic.Add(key, "value");
    if (dic.ContainsKey(key))
    {
        string data = dic[key];
    }

