
        static void Main(string[] args)
        {
            int[] targetArray = new int[] { 1, 10, 100, 1000, 10000 };
            foreach (var i in targetArray)
            {
                Console.WriteLine(i.ToString());
            }
            // 1
            // 10
            // 100
            // 1000
            // 10000

            Console.ReadKey();

        }

