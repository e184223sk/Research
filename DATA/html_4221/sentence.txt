More than 1 year has passed since last update.コレクション (IEnumerable&lt;T&gt;) の拡張メソッドを分類LINQ と同じ名前のメソッドがあり、LINQ のメソッドを呼ぶ場合に使用する。Dictionary&lt;TKey, IEnumerable&lt;TValue&gt;&gt; のような型に変換する。どのように集計するかの方法を指定する統計関数で、下記例では合計を計算するため Sum と同じ結果となる。


