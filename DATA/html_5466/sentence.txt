More than 3 years have passed since last update.与えられた変数やリテラルを処理するための記号。(「+」や「=」など)
演算子によって処理される変数やリテラルを被演算子(オペランド)という。浮動小数点の演算
2進数では無限循環小数なることがあるため、以下のような結果になる場合があるdecimal型や10で除算するなどしてを使うことで回避することは可能。decimal型はdouble型より桁数が多く、　内部的に10進数で演算する利点があるが、有効範囲が狭く、double型と比べて処理が低速である。同一性と同値性
同一性(Identity) : オブジェクト参照同士が同じオブジェクトを参照している。
同値性(Equivalence) : オブジェクトが同じ値を持っている。型違いの equals メソッドには注意浮動小数点の比較配列の比較三項演算子null合体演算子
nullの場合だけ右側の値を返す 。ビット演算子
&amp;
&lt;&lt;
~その他の演算子
sizeof : 値型のサイズをバイト単位で返す
nameof : 変数名を返すunsafeモード
ポインタ型のような規定では無効かされている機能を有効化できる。


