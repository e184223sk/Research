A a= new A();  // 参照型 どっかに中身1をつくってその参照(アドレス)をaに格納
A a2= new A(); // 参照型 どっかに中身1をつくってその参照(アドレス)をa2に格納

a2.content=2;  // a2の中身を2にする
a = a2;        // aにa2が格納している参照(アドレス)をコピーする
a.content=3;   // aの中身を3にする

// この段階で 
// a.content==3 
// a2.content==3


