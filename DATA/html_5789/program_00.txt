public void トリガー()
{
    switch( _状態変数 ){  // 今の状態が、
    case  状態1:  // 状態1の時に、
        if( ガード条件 == 真 ){  // ガード条件が真ならば、
            状態1のexitアクション();
            遷移アクション();
            状態2のentryアクション();
            状態2のdoアクティビティ();
        }
        break;
        // 略

