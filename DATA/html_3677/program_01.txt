
// [C#7.0]
// is式の拡張
// 後続の変数に null が代入されることはない。
if (obj is MyClass my) {
    // コンパイラがフローのチェックを行う
    my.ToString();
}
else {
    // コンパイラがフローのチェックを行うので、
    // ここでは変数 my が未割当と判断される
    // my.ToString(); //ここではmyが未割当
}

