More than 1 year has passed since last update.WPF（C#）でMultiBindingで、複数の値をバインディングする方法についてまとめています。プロパティがTextの場合は、StringFormatが利用できます。
プロパティがContextの場合は、StringFormatが利用できないので、Converterを介する必要があります。StringFormatが利用できるときは利用して、利用できないときはConverterを使用するといいです。


