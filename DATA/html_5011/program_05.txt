

        public void Init(float speed, float incSpeed)
        {
            mSpeed = speed;
            mIncSpeed = incSpeed;
            mAi.ApplySpeed(mSpeed);

ここを削除  IsActive = true;
        }


